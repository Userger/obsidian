словарь - это объект, где элементы хранятся по ключам.

example: 
```python
{
	"hello": "world"
}
```

[[объединение словарей {**first, **second}]]
[[словарь из списка (dict) (list)]]
[[методы словаря (dict) (methods)]]